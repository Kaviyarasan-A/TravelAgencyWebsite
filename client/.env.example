# Copy this to `client/.env` — in dev you can leave it empty (Vite proxies /api to the server).
# In production, set this to the deployed server URL, e.g. https://api.wanderlux-travel.com
VITE_API_URL=
